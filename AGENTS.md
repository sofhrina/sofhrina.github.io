# Repository workflow

任何对本网站仓库的修改都必须遵循以下流程：

1. 从最新的 `main` 创建以 `codex/` 开头的新分支。
2. 在该分支完成修改和必要检查。
3. 创建清晰、单一目的的 Git commit。
4. 将分支 push 到 GitHub。
5. 创建 Pull Request；不得把网站改动直接 push 到 `main`。
6. 在 PR 合并并部署后，再检查线上页面。

不要把 Cloudflare、GitHub、邮箱或其他服务的密钥提交到仓库。
